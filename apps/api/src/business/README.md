# business/

Reserved for cross-industry business primitives (Phase 2-5 of the PRD):
contacts, inventory, booking, payments, products, invoices, crm,
documents, calendar, tasks.

Not yet implemented. 
Each will follow the same `domain/application/infrastructure/presentation` layering as `core/*` 
once built — see `core/branch` for the reference pattern.
